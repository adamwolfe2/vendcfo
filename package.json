{
  "name": "vendcfo",
  "private": true,
  "workspaces": ["packages/*", "apps/*", "packages/email/*"],
  "scripts": {
    "build": "turbo build",
    "clean": "git clean -xdf node_modules",
    "clean:workspaces": "turbo clean",
    "dev": "turbo dev --parallel",
    "test": "turbo test --parallel",
    "start:dashboard": "turbo start --filter=@vendcfo/dashboard",
    "start:website": "turbo start --filter=@vendcfo/website",
    "dev:api": "turbo dev --filter=@vendcfo/api",
    "dev:dashboard": "turbo dev --filter=@vendcfo/dashboard",
    "build:dashboard": "turbo build --filter=@vendcfo/dashboard",
    "dev:engine": "turbo dev --filter=@vendcfo/engine",
    "dev:website": "turbo dev --filter=@vendcfo/website ",
    "dev:desktop": "turbo dev --filter=@vendcfo/desktop",
    "jobs:dashboard": "turbo jobs --filter=@vendcfo/jobs",
    "format": "biome format --write .",
    "lint": "turbo lint && manypkg check",
    "typecheck": "turbo typecheck"
  },
  "dependencies": {
    "@biomejs/biome": "1.9.4",
    "@manypkg/cli": "^0.25.1",
    "turbo": "2.7.0",
    "typescript": "^5.9.3"
  },
  "packageManager": "bun@1.2.22"
}
